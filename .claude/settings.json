{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read file_path; if echo \"$file_path\" | grep -q '\\.py$'; then uv run black \"$file_path\" && uv run isort \"$file_path\"; fi; }"
          }
        ]
      }
    ]
  },
  "permissions": {
    "deny": ["Bash(sudo *)"]
  }
}
